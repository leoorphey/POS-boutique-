services:
  postgres:
    image: postgres:16-alpine
    container_name: pos_boutique_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: pos_user
      POSTGRES_PASSWORD: pos_password
      POSTGRES_DB: pos_boutique
    ports:
      - "5432:5432"
    volumes:
      - pos_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pos_user -d pos_boutique"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pos_postgres_data:
