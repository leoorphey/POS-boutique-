{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build:shared && npx prisma generate --schema=server/prisma/schema.prisma && npm run build:server"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=server/prisma/schema.prisma && npm run start --workspace=server",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
