{
  "framework": null,
  "buildCommand": "npm install && npm run build:shared && ls -la shared/dist/ && cat shared/dist/index.js && npm run build:client",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'skip - handled in buildCommand'",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}